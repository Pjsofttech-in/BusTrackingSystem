spring:
  application:
    name: bustracking

  datasource:
    url: jdbc:mysql://localhost:3306/bustrackdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true